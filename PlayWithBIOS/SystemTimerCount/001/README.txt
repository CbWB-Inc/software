SystemTimerCount	001

  System Timerを読みだして内容を表示しする。
  それだけのプログラムですね。
  BIOSコールで何が返ってくるか確認しています。


SystemTimerCount 001

  This program reads the system timer and displays its value.
  That's all it does.

  The purpose is to check exactly what the BIOS call returns.
