# echo

## 🗾 概要（Japanese）

拡張ディスクリードの実装実験です。
dap構造体に固定設定したパラメータでディスクをリードしてみます。


---

## 🌐 Overview（English）

This is an experimental implementation of extended disk read.
It attempts to read from the disk using fixed parameters set in the DAP structure.
